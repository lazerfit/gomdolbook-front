import ErrorPage from '@/components/templates/ErrorPage';

const NotFoundPage = () => {
  return <ErrorPage title="404" subTitle="The page you're looking for doesn't exist." />;
};

export default NotFoundPage;
