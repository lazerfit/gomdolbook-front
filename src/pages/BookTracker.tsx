import styled from 'styled-components'
import '../styles/variables.css'
import Tracker from '../components/tracker/Tracker'

type Props = {}

const BookTracker = (props: Props) => {
  return <Tracker />
}

export default BookTracker
